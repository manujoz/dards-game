# Docker build context ignore rules
# Keep this conservative: exclude heavy/derived artifacts and secrets,
# but keep Prisma schema/migrations and source code.

# Dependencies (installed inside image)
node_modules

# Next.js build output (built inside image)
.next
out

# Test/coverage outputs
coverage

# Build/tool caches
.turbo
.cache
.swc
.eslintcache

# Logs
*.log
npm-debug.log*
pnpm-debug.log*
yarn-debug.log*
yarn-error.log*

# Env files (should be provided via docker-compose/env, not copied)
.env
.env.*

# OS/editor noise
.DS_Store
Thumbs.db
.vscode
.idea

# Git metadata (not needed in image)
.git
.gitignore

# Prisma local SQLite files (keep schema + migrations!)
prisma/*.db
prisma/*.db-*
prisma/*.db-journal
prisma/*.db-shm
prisma/*.db-wal

# Misc
*.tmp
*.temp
